argcomplete>=3.1.4,<4.0.0        # script's args
websocket-client>=1.6.0,<2.0.0  # HA WebSocket API (discover devices from HA registry)
pyyaml>=6.0.1,<7.0.0            # config files
webcolors>=24.6.0,<25.0.0       # get names for RGB colors
defusedxml>=0.7.1,<1.0.0        # safe XML parsing (prevents XXE / entity-expansion attacks)

# Network Communication
httpx>=0.27.0,<1.0.0             # HTTP client for Python

# Hue communication
# https://github.com/hrdasdominik/hue-entertainment-pykit
# dependencies: https://github.com/hrdasdominik/hue-entertainment-pykit/blob/main/pyproject.toml
# hue-entertainment-pykit requires Python<=3.12
hue-entertainment-pykit>=0.1.0,<1.0.0

# python-mbedtls==2.10.1  # the newest version # https://pypi.org/project/python-mbedtls/2.10.1/
